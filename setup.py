from setuptools import setup, find_packages

setup(
    name='logguard',
    version='0.1.0',
    description='A logging wrapper for sanitizing sensitive information before logging.',
    author='Akhilesh Bhogale',
    packages=find_packages(),
    install_requires=[],
)
